# ===========================================
# AutoShorts 프로젝트 환경변수 설정
# ===========================================
# 이 파일을 .env로 복사하고 실제 값들을 입력하세요

# ===========================================
# API Keys (필수)
# ===========================================
# OpenAI API 키 - GPT 모델 사용을 위해 필요
# https://platform.openai.com/api-keys 에서 발급
OPENAI_API_KEY=your_openai_api_key_here

# Google API 키 - Gemini 모델 사용을 위해 필요
# https://makersuite.google.com/app/apikey 에서 발급
GOOGLE_API_KEY=your_google_api_key_here

# Google Cloud TTS 인증 파일 경로 - 음성 합성을 위해 필요
# Google Cloud Console에서 서비스 계정 키를 다운로드하고 경로 지정
GOOGLE_APPLICATION_CREDENTIALS=./google-service-account.json

# ===========================================
# 서버 설정
# ===========================================
# 백엔드 서버 포트
PORT=3001

# 환경 모드 (development, production)
NODE_ENV=development

# ===========================================
# Frontend URLs (Next.js - NEXT_PUBLIC_ 접두사 필수)
# ===========================================
# 백엔드 API 기본 URL
NEXT_PUBLIC_API_URL=http://localhost:3001

# 파일 업로드 엔드포인트
NEXT_PUBLIC_UPLOAD_URL=http://localhost:3001/api/upload

# PDF 파싱 엔드포인트
NEXT_PUBLIC_PARSE_URL=http://localhost:3001/api/parse

# 슬라이드 그룹화 엔드포인트
NEXT_PUBLIC_GROUP_URL=http://localhost:3001/api/group-slides

# ===========================================
# 추가 설정 (선택사항)
# ===========================================
# 이미지 생성 서비스 URL (필요시)
# NEXT_PUBLIC_IMAGE_URL=http://localhost:3001/api/generate-image

# TTS 서비스 URL (필요시)
# NEXT_PUBLIC_TTS_URL=http://localhost:3001/api/tts

# 스크립트 생성 서비스 URL (필요시)
# NEXT_PUBLIC_SCRIPT_URL=http://localhost:3001/api/generate-script
